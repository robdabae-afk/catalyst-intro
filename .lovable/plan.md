Add a button below the success message in `src/pages/EventSignIn.tsx`.

- Text: "Join the Catalyst platform"
- Action: navigate to `/app` using `window.location.href` (consistent with existing mobile/PWA navigation pattern)
- Styling: match the existing dark theme, white background with black text or outline style for contrast
- Placement: below the "Welcome. Grab a drink & enjoy the event!" paragraph inside the centered success block